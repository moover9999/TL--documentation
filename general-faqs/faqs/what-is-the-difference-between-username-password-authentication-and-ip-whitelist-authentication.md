# What is the difference between username/password authentication and IP whitelist authentication?

* **Username/Password:** Use any device with the provided credentials; convenient for dynamic IPs.
* **IP Whitelist:** Only devices with pre-approved IPs can access proxies; more secure but less flexible.
